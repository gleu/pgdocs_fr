<!-- Dernière modification
     le       $Date$
     par      $Author$
     révision $Revision$ -->

<sect1>
 <title>adminpack</title>

 <indexterm zone="adminpack">
  <primary>adminpack</primary>
 </indexterm>

 <para>
  <filename>adminpack</filename> implémente un certain nombre de fonctions de
  support que <application>pgAdmin</application> et d'autres outils de gestion
  et d'administration peuvent utiliser pour fournir des fonctionnalités
  supplémentaires, comme la gestion à distance de journaux applicatifs.
 </para>

 <sect2>
  <title>Fonctions implémentées</title>
  <para>
   Les fonctions ajoutées par <filename>adminpack</filename> peuvent seulement
   être exécutées par un super-utilisateur. Voici la liste des fonctions&nbsp;:

<programlisting>
int8 pg_catalog.pg_file_write(fname text, data text, append bool)
bool pg_catalog.pg_file_rename(oldname text, newname text, archivename text)
bool pg_catalog.pg_file_rename(oldname text, newname text)
bool pg_catalog.pg_file_unlink(fname text)
setof record pg_catalog.pg_logdir_ls()

/* Renommage des fonctions internes existantes pour une compatibilité
avec pgAdmin */
int8 pg_catalog.pg_file_read(fname text, data text, append bool)
bigint pg_catalog.pg_file_length(text)
int4 pg_catalog.pg_logfile_rotate()
</programlisting>

  </para>

 </sect2>

</sect1>
