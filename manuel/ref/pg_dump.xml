<?xml version="1.0" encoding="ISO-8859-1"?>
<!--
$Header: /var/lib/cvs/pgsql-fr/sgml/ref/pg_dump.sgml,v 1.13 2005/09/15 07:03:27 guillaume Exp $
PostgreSQL documentation
-->
<!-- SAS 20060530 : Relecture initiale -->

<refentry id="app-pgdump">
 <refmeta>
  <refentrytitle>pg_dump</refentrytitle>
  <manvolnum>1</manvolnum>
  <refmiscinfo>Application</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>pg_dump</refname>

  <refpurpose>
   sauvegarder une base de données <productname>PostgreSQL</productname> dans un
   script ou tout autre fichier d'archive
  </refpurpose>
 </refnamediv>

 <refsynopsisdiv>
  <cmdsynopsis>
   <command>pg_dump</command>
   <arg rep="repeat"><replaceable>option</replaceable></arg>
   <arg><replaceable>nom_base</replaceable></arg>
  </cmdsynopsis>
 </refsynopsisdiv>


 <refsect1 id="pg-dump-description">
  <title>
   Description
  </title>
  <indexterm zone="app-pgdump">
   <primary>pg_dump</primary>
  </indexterm>


  <para>
   <application>pg_dump</application> est un outil de sauvegarde d'une base de
   données <productname>PostgreSQL</productname>. Les sauvegardes 
   réalisées sont cohérentes, même lors d'accès concurrents à la base de données.
   <application>pg_dump</application> ne bloque pas l'accès des autres
   utilisateurs (ni en lecture ni en écriture).
  </para>

  <para>
   Les extractions peuvent être réalisées sous la forme de scripts ou de fichiers d'archive.
   Les scripts sont au format texte et contiennent les commandes SQL
   nécessaires à la reconstruction de la base de données dans l'état où elle était au
   moment de la sauvegarde. La restauration s'effectue en chargeant ces scrits avec
   <xref linkend="app-psql"/>. Ces scripts permettent de reconstruire la base de
   données sur d'autres machines et d'autres architectures, et même, au prix de
   quelques modifications, sur d'autres bases de données SQL.
  </para>

  <para>
   La reconstruction de la base de données à partir d'autres formats de fichiers archive
   est obtenue avec <xref linkend="app-pgrestore"/>. 
   <application>pg_restore</application> permet, à partir de ces formats, de sélectionner
   les éléments à restaurer, voire de les réordonner avant restauration.
   Les fichiers d'archive sont conçus pour être portables au travers d'architectures différentes.
  </para>

  <para>
   Lorsqu'il est utilisé avec un des formats de fichier d'archivé en combinaison
   avec <application>pg_restore</application>,
   <application>pg_dump</application> fournit un mécanisme d'archivage
   et de transfert flexible. <application>pg_dump</application> peut être utilisé pour
   sauvegarder une base de données dans son intégralité&nbsp;;
   <application>pg_restore</application> peut alors être utilisé pour examiner
   l'archive et/ou sélectionner les parties de la base de données à
   restaurer. Le format de fichier de sortie le plus flexible est le format
   <quote>personnalisé</quote> (<foreignphrase>custom</foreignphrase> en anglais, <option>-Fc</option>).
   Compressé par défaut, il permet de sélectionner et réordonner les éléments archivés.
   Nom compressé, le format <application>tar</application> (<option>-Ft</option>)
   ne permet pas de réordonner les données au
   chargement mais est somme toute assez flexible. Qui plus est, il est utilisable
   par des outils Unix standard comme <command>tar</command>.
  </para>

  <para>
   Lors de l'exécution de <application>pg_dump</application>, il est utile de 
   surveiller les messages d'avertissement (affichés sur la sortie erreur standard), 
   en particulier en ce qui concerne les limitations indiquées ci-dessous.
  </para>

 </refsect1>

 <refsect1 id="pg-dump-options">
  <title>Options</title>

  <para>
   Les options suivantes de la ligne de commande contrôlent le contenu et le
   format de la sortie.

    <variablelist>
     <varlistentry>
      <term><replaceable class="parameter">nom_base</replaceable></term>
      <listitem>
       <para>
	Le nom de la base de données à sauvegarder. En l'absence de précision, 
	la variable d'environnement <envar>PGDATABASE</envar> est utilisée. 
        Si cette variable n'est pas positionnée, le nom de l'utilisateur de la connexion est utilisé.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-a</option></term>
      <term><option>--data-only</option></term>
      <listitem>
       <para>
	Seules les données sont sauvegardées, pas le schéma (définition des	données).
       </para>

       <para>
        Cette option n'a d'intérêt que pour le format texte.
	Pour les formats archive, l'option est spécifiée à l'appel de <command>pg_restore</command>.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-c</option></term>
      <term><option>--clean</option></term>
      <listitem>
       <para>
        Les commandes de nettoyage (suppression) des objets de la base
        sont écrites avant les commandes de création.
       </para>

       <para>
        Cette option n'a d'intérêt que pour le format texte. Pour les
	formats archive, l'option est spécifiée à l'appel de
	<command>pg_restore</command>.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-C</option></term>
      <term><option>--create</option></term>
      <listitem>
       <para>
        La sortie débute par une commande de création de la base de données
	et de connexion à cette base. Peu importe dans ce cas la base de données
        à laquelle la connexion est faite avant la restauration.
       </para>

       <para>
        Cette option n'a d'intérêt que pour le format texte. Pour les
	formats archive, l'option est spécifiée à l'appel de
	<command>pg_restore</command>.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-d</option></term>
      <term><option>--inserts</option></term>
      <listitem>
       <para>
        Les données sont copiées à l'aide de commandes <command>INSERT</command>
        plutôt que <command>COPY</command>. La restauration en est considérablement
        ralentie. Cela s'avère essentiellement utile pour créer des sauvegardes 
        rechargées dans des bases de données autres que
        <productname>PostgreSQL</productname>. La restauration
        échoue tout de même si les colonnes ont été réorganisées. L'option
        <option>-D</option> est plus sûre, mais encore plus lente.
        De plus, même si cette option engendre une erreur à chaque
        donnée invalide, les autres commandes <command>INSERT</command>
        continuent le chargement des données dans la table.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-D</option></term>
      <term><option>--column-inserts</option></term>
      <term><option>--attribute-inserts</option></term>
      <listitem>
       <para>
        Les données sont copiées à l'aide de commandes <command>INSERT</command>
        qui explicitent les noms de colonnes 
        (<literal>INSERT INTO <replaceable>table</replaceable> (<replaceable>colonne</replaceable>, ...) VALUES ...</literal>).
        La restauration en est considérablement ralentie. Cela s'avère essentiellement utile pour créer des sauvegardes
        rechargées dans des bases de données autres que
        <productname>PostgreSQL</productname>. 
        De plus, même si cette option engendre une erreur à chaque
        donnée invalide, les autres commandes <command>INSERT</command>
        continuent le chargement des données dans la table.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-f <replaceable class="parameter">file</replaceable></option></term>
      <term><option>--file=<replaceable class="parameter">file</replaceable></option></term>
      <listitem>
       <para>
		La sortie est redirigée vers le fichier spécifié. Sans précision, la sortie
    	standard est utilisée.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
	  <term><option>-E <replaceable class="parameter">codage</replaceable></option></term>
	  <term><option>--encoding=<replaceable class="parameter">codage</replaceable></option></term>
	  <listitem>
	   <para>
	    La sauvegarde est créée dans l'encodage spécifié. Par défaut, la sauvegarde utilise celui
	    de la base de données. Le même résultat peut être obtenu en positionnant 
	    la variable d'environnement <envar>PGCLIENTENCODING</envar> avec le codage désiré pour la
	    sauvegarde.
	   </para>
	  </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-F <replaceable class="parameter">format</replaceable></option></term>
      <term><option>--format=<replaceable class="parameter">format</replaceable></option></term>
      <listitem>
       <para>
        Le format de la sortie.
	    <replaceable>format</replaceable> correspond à un des éléments suivants&nbsp;:

       <variablelist>
        <varlistentry>
         <term><literal>p</literal></term>
         <listitem>
          <para>
           Fichier de scripts <acronym>SQL</acronym> en texte simple (défaut).
          </para>
         </listitem>
        </varlistentry>

        <varlistentry>
         <term><literal>t</literal></term>
         <listitem>
          <para>
           Archive <command>tar</command> utilisable par <application>pg_restore</application>.
           Ce format permet d'ordonner et/ou d'exclure des objets lors
           de la restauration de la base. 
           Il est également possible de restreindre les données rechargées lors de la restauration.
          </para>
         </listitem>
        </varlistentry>

        <varlistentry>
         <term><literal>c</literal></term>
         <listitem>
          <para>
           Archive personnalisée utilisable par <application>pg_restore</application>.
           C'est le format le plus flexible puisqu'il permet d'ordonner le chargement des
	       données et la définition des objets. Ce format est compressé par défaut.
          </para>
         </listitem>
        </varlistentry>

       </variablelist>
       </para>

      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-i</option></term>
      <term><option>--ignore-version</option></term>
      <listitem>
       <para>
        Les différences de version entre
        <application>pg_dump</application> et le serveur de bases de données sont ignorées.
       </para>

       <para>
        <application>pg_dump</application> peut gérer des bases de données de
		versions différentes, mais les très anciennes versions ne sont plus
		supportées (actuellement celles antérieures à la 7.0). Cette option est
		utilisée lorsque la vérification de version ne doit pas être effectuée.
		Dans ce cas, il se peut que <application>pg_dump</application> échoue.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-n <replaceable class="parameter">schéma</replaceable></option></term>
      <term><option>--schema=<replaceable class="parameter">schéma</replaceable></option></term>
      <listitem>
       <para>
        Seul le contenu de <replaceable	class="parameter">schéma</replaceable> est
		sauvegardé. Si cette option n'est pas précisée, tous
		les schémas de la base de données considérée, autres que ceux systèmes, sont sauvegardés.
       </para>

       <note>
        <para>
         Dans ce mode, <application>pg_dump</application> ne 
		 sauvegarde aucun objet de quelque autre base de données dont peuvent 
		 dépendre les objets du schéma sélectionné. 
	 	 De ce fait, aucune garantie n'est donnée quant au succès d'une restauration
	     dans une base vierge des données issues de la sauvegarde du seul schéma.
        </para>
       </note>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-o</option></term>
      <term><option>--oids</option></term>
      <listitem>
       <para>
	  	Les identifiants d'objets (<acronym>OID</acronym>) sont sauvegardés comme
	  	donnée des tables. Cette option est utilisée dans le cas d'applications
		utilisant des références aux colonnes <acronym>OID</acronym> 
	    (dans une contrainte de clé étrangère, par exemple). Elle ne devrait pas être
	    utilisée dans les autres cas.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-O</option></term>
      <term><option>--no-owner</option></term>
      <listitem>
       <para>
        Les commandes d'initialisation des possessions des objets au regard de
		la base de données originale ne sont pas produites. Par défaut, 
		<application>pg_dump</application> engendre des instructions
		<command>ALTER OWNER</command> ou 
		<command>SET SESSION AUTHORIZATION</command> pour
		fixer ces possessions. 
		Ces instructions échouent lorsque le script n'est pas
		lancé par un superutilisateur (ou par l'utilisateur
		qui possède tous les objets de ce script). L'option <option>-O</option>
		est utilisée pour créer un script qui puisse être restauré par n'importe 
		quel utilisateur. En revanche, c'est cet utilisateur qui devient 
		propriétaire de tous les objets.
       </para>

       <para>
        Cette option n'a d'intérêt que pour le format texte.
		Pour les formats archive, l'option est spécifiée à l'appel de <command>pg_restore</command>.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-R</option></term>
      <term><option>--no-reconnect</option></term>
      <listitem>
       <para>
        Cette option, obsolète, est toujours acceptée pour des raisons
		de compatibilité ascendante.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-s</option></term>
      <term><option>--schema-only</option></term>
      <listitem>
       <para>
		Seule la définition des objets (le schéma) est sauvegardée, pas les
		données.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-S <replaceable 	class="parameter">nomutilisateur</replaceable></option></term>
      <term><option>--superuser=<replaceable 	class="parameter">nomutilisateur</replaceable></option></term>
      <listitem>
       <para>
        Le nom du superutilisateur à utiliser lors de la désactivation
		des déclencheurs. Cela n'a d'intérêt que si l'option
		<option>--disable-triggers</option> est précisée. (En règle générale, 
		il est préférable de ne pas utiliser cette option et de lancer le script
		engendré en tant que superutilisateur.)
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-t <replaceable class="parameter">table</replaceable></option></term>
      <term><option>--table=<replaceable class="parameter">table</replaceable></option></term>
      <listitem>
       <para>
		Seules les données <replaceable	class="parameter">table</replaceable> sont
		sauvegardées. Il se peut que plusieurs tables aient le même nom dans
		des schémas différents. Dans ce cas, toutes les tables correspondantes sont
		sauvegardées. Pour ne sélectionner qu'une seule table, à la fois
		<option>--schema</option> et <option>--table</option>
		sont précisés.
       </para>

       <note>
        <para>
         Dans ce mode, <application>pg_dump</application> ne sauvegarde aucun
		 objet d'autres bases de données dont la table sélectionnée peut dépendre.
		 De ca fait, aucune garantie n'est données sur le succès de la restauration
		 dans une base vierge de la sauvegarde des données de cette seule table.
        </para>
       </note>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-v</option></term>
      <term><option>--verbose</option></term>
      <listitem>
       <para>
		Mode verbeux. <application>pg_dump</application> affiche des commentaires
		détaillés sur les objets et les heures de début et de fin dans le fichier de
		sauvegarde. Des messages de progression sont également affichés sur la 
		sortie erreur standard.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-x</option></term>
      <term><option>--no-privileges</option></term>
      <term><option>--no-acl</option></term>
      <listitem>
       <para>
		Les droits d'accès (commandes grant/revoke) ne sont pas sauvegardés.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-X disable-dollar-quoting</option></term>
      <term><option>--disable-dollar-quoting</option></term>
      <listitem>
       <para>
		Cette option désactive l'utilisation des guillemets dollar pour le
		corps des fonctions. La syntaxe SQL standard des chaînes est utilisée pour
		la mise entre guillemets du corps des fonctions.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-X disable-triggers</option></term>
      <term><option>--disable-triggers</option></term>
      <listitem>
       <para>
        Cette option n'a d'intérêt que dans le cas de sauvegarde des données seules.
		Dans ce cas, <application>pg_dump</application> inclut à la sortie des
		commandes permettant de désactiver temporairement les déclencheurs sur les tables
		cibles lors du chargement des données.
		Cette commande est utilisée lorsqu'il existe sur les tables des vérifications d'intégrité
		référentielles ou des déclencheurs dont l'exécution n'est pas souhaitable
		lors du chargement des données.
       </para>

       <para>
        À l'heure actuelle, les commandes engendrées par <option>--disable-triggers</option>
		doivent être exécutées par le superutilisateur. Il est donc nécessaire que le nom
		du superutilisateur soit également précisé, à l'aide de <option>-S</option>. 
		Le script résultant peut aussi être lancé par le superutilisateur (c'est d'ailleurs
		préférable).
       </para>

       <para>
        Cette option n'a d'intérêt que pour le format texte. Pour les
	    formats archive, l'option est spécifiée à l'appel de
	    <command>pg_restore</command>.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-X use-set-session-authorization</option></term>
      <term><option>--use-set-session-authorization</option></term>
      <listitem>
       <para>
		Les commandes <command>SET SESSION AUTHORIZATION</command> du standard
		SQL sont affichées à la place des commandes <command>ALTER OWNER</command> pour
		préciser le propriétaire d'un objet. Cela rend la sauvegarde plus compatible
		avec le standard mais, en fonction de l'historique des objets dans la sauvegarde,
		il se peut que la restauration soit incorrecte. De plus, une sauvegarde qui utilise
		<command>SET SESSION AUTHORIZATION</command> nécessite pour la restauration des droits
		superutilisateur tandis que	<command>ALTER OWNER</command> requiert des privilèges
		moins étendus.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-Z <replaceable class="parameter">0..9</replaceable></option></term>
      <term><option>--compress=<replaceable class="parameter">0..9</replaceable></option></term>
      <listitem>
       <para>
		Le niveau de compression, si le format d'archive la supporte.
		Actuellement, seul le format personnalisé supporte la compression.
       </para>
      </listitem>
     </varlistentry>
    </variablelist>
   </para>

   <para>
    Les options de ligne de commande suivantes gèrent les paramètres de connexion&nbsp;:

    <variablelist>
     <varlistentry>
      <term><option>-h <replaceable	class="parameter">hôte</replaceable></option></term>
      <term><option>--host <replaceable	class="parameter">hôte</replaceable></option></term>
      <listitem>
       <para>
        Le nom d'hôte de la machine sur laquelle le serveur de bases
        de données est exécuté. Si la valeur commence par une barre oblique (/),
        elle est utilisée comme répertoire pour le socket de domaine Unix.
		La valeur par défaut est fournie par la variable d'environnement
		<envar>PGHOST</envar>, si elle est initialisée. Dans le cas contraire, 
		une connexion sur la socket de domaine Unix est tentée.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-p <replaceable class="parameter">port</replaceable></option></term>
      <term><option>--port <replaceable class="parameter">port</replaceable></option></term>
      <listitem>
       <para>
        Le port TCP ou le fichier local de socket de domaine Unix
        sur lequel le serveur écoute les connexions.
	    La valeur par défaut est fournie par la variable d'environnement
		<envar>PGPORT</envar>, si elle est initialisée. Dans le cas contraire,
		il s'agit de la valeur fournie à la compilation.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-U <replaceable class="parameter">nomutilisateur</replaceable></option></term>
      <term><option>--username <replaceable class="parameter">nomutilisateur</replaceable></option></term>
      <listitem>
       <para>
        Le nom d'utilisateur utilisé pour la connexion.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry>
      <term><option>-W</option></term>
      <term><option>--password</option></term>
      <listitem>
       <para>
        Un mot de passe est demandé dans tous les cas. Ce comportement est celui automatiquement obtenu
		lorsque le serveur requiert une authentification par mot de passe.
       </para>
      </listitem>
     </varlistentry>
    </variablelist>
   </para>
 </refsect1>

 <refsect1>
  <title>Environnement</title>

  <variablelist>
   <varlistentry>
    <term><envar>PGDATABASE</envar></term>
    <term><envar>PGHOST</envar></term>
    <term><envar>PGPORT</envar></term>
    <term><envar>PGUSER</envar></term>

    <listitem>
     <para>
      Paramètres de connexion par défaut.
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1 id="app-pgdump-diagnostics">
  <title>Diagnostiques</title>

  <para>
   <application>pg_dump</application> exécute intrinsèquement des instructions
   <command>SELECT</command>. Si des problèmes apparaissent à l'exécution de
   <application>pg_dump</application>, <xref linkend="app-psql"/> peut être 
   utilisé pour s'assurer de la possibilité de sélectionner des informations
   dans la base de données.
  </para>
 </refsect1>


 <refsect1 id="pg-dump-notes">
  <title>Notes</title>

  <para>
   Si des ajouts locaux à la base <literal>template1</literal> ont été effectués
   dans le groupe de bases de données, il est impératif de s'assurer que la 
   sortie de <application>pg_dump</application> est effectivement restaurée
   dans une base vide&nbsp;; dans le cas contraire, il est fort probable 
   que la duplication des définitions des objets ajoutés engendre des erreurs.
   Pour obtenir une base vide de tout ajout local, on utilise <literal>template0</literal>
   à la place de <literal>template1</literal> comme modèle.
   Par exemple&nbsp;:
<programlisting>CREATE DATABASE foo WITH TEMPLATE template0;
</programlisting>
  </para>

  <para>
   <application>pg_dump</application> souffre quelques limitations&nbsp;:

   <itemizedlist>
    <listitem>
     <para>
      Lorsqu'une sauvegarde des seules données est sélectionnée et que l'option
      <option>--disable-triggers</option> est utilisée,
      <application>pg_dump</application> engendre des commandes de désactivation des
      déclencheurs sur les tables utilisateur avant l'insertion des données et des
      commandes de réactivation après l'insertion. Si la
      restauration est interrompue, il se peut que les catalogues systèmes
	  conservent cette position.
     </para>
    </listitem>

   </itemizedlist>
  </para>

  <para>
   Les fichiers d'une archive tar sont limités à une taille inférieure à
   8&nbsp;Go. (C'est une limitation inhérente au format des fichiers tar.) 
   Ce format ne peut donc pas être utilisé si la représentation textuelle
   d'une table dépasse cette taille. La taille totale d'une archive tar et de
   tout autre format de sortie n'est pas limitée, sauf peut-être par le système
   d'exploitation.
  </para>

  <para>
   Le fichier de sauvegarde engendré par <application>pg_dump</application> ne
   contient pas les statistiques utilisées par l'optimiseur pour la
   planification des requêtes. Il est donc conseillé, pour assurer des
   performances correctes, de lancer <command>ANALYZE</command> après
   la restauration d'une sauvegarde.
  </para>

  <para>
   <application>pg_dump</application> est utilisé pour transférer des
   données vers les nouvelles versions de <productname>PostgreSQL</productname>.
   De ce fait, le résultat de <application>pg_dump</application> peut être chargé dans des
   bases de données <productname>PostgreSQL</productname> de versions supérieures.
   Ce résultat peut aussi être lu par des serveurs <productname>PostgreSQL</productname>
   plus anciens. Néanmoins, <application>pg_dump</application> ne peut généralement
   pas lire des bases de données plus récentes ou produire une
   sauvegarde qui puisse être chargée par des versions antérieures. Pour cela, une
   édition manuelle de la sauvegarde peut se révéler nécessaire.
  </para>
 </refsect1>

 <refsect1 id="pg-dump-examples">
  <title>Exemples</title>

  <para>
   Sauvegarder une base de données&nbsp;:
<screen><prompt>$</prompt> <userinput>pg_dump mabase &gt; base.out</userinput>
</screen>
  </para>

  <para>
   Recharger cette base de données&nbsp;:
<screen><prompt>$</prompt> <userinput>psql -d base -f base.out</userinput>
</screen>
  </para>

  <para>
   Sauvegarder une base de données nommée <literal>mabase</literal> dans un
   fichier <filename>tar</filename>&nbsp;:

<screen><prompt>$</prompt> <userinput>pg_dump -Ft mabase &gt; base.tar</userinput>
</screen>
  </para>

  <para>
   Charger cette sauvegarde dans une base de données existante appelée
   <literal>nouvellebase</literal>&nbsp;:

<screen><prompt>$</prompt> <userinput>pg_restore -d nouvellebase base.tar</userinput>
</screen>
  </para>

 </refsect1>

 <refsect1>
  <title>Historique</title>

  <para>
   L'outil <application>pg_dump</application> est apparu à l'oigine dans
   <application>Postgres95</application> version 0.02. Les formats de sortie
   non textuels ont été introduits dans la version 7.1 de
   <productname>PostgreSQL</productname>.
  </para>
 </refsect1>

 <refsect1>
  <title>Voir aussi</title>

  <simplelist type="inline">
   <member><xref linkend="app-pg-dumpall"/></member>
   <member><xref linkend="app-pgrestore"/></member>
   <member><xref linkend="app-psql"/></member>
   <member>Variables d'environnement (<xref linkend="libpq-envars"/>)</member>
  </simplelist>
 </refsect1>

</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:nil
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"../reference.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:"/usr/lib/sgml/catalog"
sgml-local-ecat-files:nil
End:
-->
