<!-- $PostgreSQL: pgsql/doc/src/sgml/passwordcheck.sgml,v 1.2 2010-04-03 07:22:55 petere Exp $ -->

<sect1 id="passwordcheck">
 <title>passwordcheck</title>

 <indexterm zone="passwordcheck">
  <primary>passwordcheck</primary>
 </indexterm>

 <para>
  Le module <filename>passwordcheck</filename> vÃ©rifie les mots de passe des
  utilisateurs quand ils sont configurÃ©s avec <xref linkend="sql-createrole"/>
  ou <xref linkend="sql-alterrole" />. Si un mot de passe est considÃ©rÃ© trop
  faible, il sera rejetÃ© et la commande terminera avec une erreur.
 </para>

 <para>
  Pour activer ce module, ajoutez <literal>'$libdir/passwordcheck'</literal>
  dans le paramÃštre <xref linkend="guc-shared-preload-libraries" /> du fichier
  <filename>postgresql.conf</filename>, puis redÃ©marrez le serveur.
 </para>

 <para>
  Vous pouvez adapter ce module Ã  vos besoins en changeant son code source.
  Par exemple, vous pouvez utiliser <ulink
  url="http://sourceforge.net/projects/cracklib/">CrackLib</ulink> pour
  vÃ©rifier les mots de passe &mdash; ceci requiert seulement la suppression
  des commentaires sur deux lignes du <filename>Makefile</filename> et la
  reconstruction du module. (Nous ne pouvons pas inclure
  <productname>CrackLib</productname> par dÃ©faut pour des raisons de licence.)
  Sans <productname>CrackLib</productname>, le module force quelques rÃšgles
  simples sur la force du mot de passe, que vous pouvez modifier ou Ã©tendre
  au besoin.
 </para>

 <caution>
  <para>
   Pour empÃªcher l'envoi en clair de mot de passe sur le rÃ©seau, leur Ã©criture
   dans les journaux applicatifs ou leur rÃ©cupÃ©ration par un administrateur de
   bases de donnÃ©es, <productname>PostgreSQL</productname> permet Ã 
   l'utilisateur de fournir des mots de passe dÃ©jÃ  chiffrÃ©s. Beaucoup de
   programmes clients utilisent cette fonctionnalitÃ© et chiffrent le mot de
   passe avant de l'envoyer au serveur.
  </para>
  <para>
   Ceci limite l'utilitÃ© du module <filename>passwordcheck</filename> car,
   dans ce cas, il peut seulement tenter de deviner le mot de passe. Pour cette
   raison, <filename>passwordcheck</filename> n'est pas recommandÃ© si vos
   besoins en sÃ©curitÃ© sont importants. Il est plus intÃ©ressant d'utiliser une
   mÃ©thode d'authentification externe comme Kerberos (voir <xref
   linkend="client-authentication" />) plutÃŽt que de se fier aux mots de passe
   internes.
  </para>
  <para>
   Autrement, vous pouvez modifier <filename>passwordcheck</filename> pour
   rejeter les mots de passe prÃ©-chiffrÃ©s, mais forcer les utilisateurs Ã 
   initialiser les mots de passe en clair apporte son lot de problÃšmes de
   sÃ©curitÃ©.
  </para>
 </caution>

</sect1>
