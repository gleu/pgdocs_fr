<!-- doc/src/sgml/release-9.2.sgml -->
<!-- See header comment in release.sgml about typical markup -->

 <sect1 id="release-9-2">
  <title>Release 9.2</title>

  <note>
   <title>Release Date</title>
   <simpara>2012-??-??</simpara>
   <simpara>CURRENT AS OF 2012-05-09</simpara>

  </note>

  <sect2>
   <title>Overview</title>

   <para>
    NARRATIVE HERE. Major enhancements include:
   </para>

   <itemizedlist>
    <listitem><para>MAJOR LIST HERE</para></listitem>
    <!-- This list duplicates items below, but without authors or details-->

   </itemizedlist>

   <para>
    The above items are explained in more detail in the sections below.
   </para>

  </sect2>

  <sect2>

  <title>Migration to Version 9.2</title>

   <para>
    A dump/restore using <application>pg_dump</application>, or use of
    <application>pg_upgrade</application>, is required for those wishing
    to migrate data from any previous release.
   </para>

   <para>
    Version 9.2 contains a number of changes that may affect compatibility
    with previous releases.  Observe the following incompatibilities:
   </para>

   <sect3>
    <title>Functions</title>

    <itemizedlist>

      <listitem>
       <para>
        Ensure that <link
        linkend="functions-xml-processing"><function>xpath()</function></link>
        escapes special characters in string values (Florian Pflug)
       </para>

       <para>
        Without this it is possible to output illegal
        <acronym>XML</acronym>.
       </para>
      </listitem>

      <listitem>
       <para>
        Remove <link linkend="hstore">hstore</link>'s <literal>=&gt;</literal>
        operator (Robert Haas)
       </para>

       <para>
        Users should now use hstore(text, text).  Since
        <productname>PostgreSQL</productname> 9.0, a warning message is
        emitted when an operator named =&gt; is created because
        the <acronym>SQL</acronym> standard reserves that token for
        another use.
       </para>
      </listitem>

      <listitem>
       <para>
        Have <link
        linkend="functions-admin-dbobject"><function>pg_relation_size()</function></link>
        and friends return NULL if the object does not exist (Phil Sorber)
       </para>

       <para>
        This prevents queries that call these functions from returning
        errors during concurrent drop operations.
       </para>
      </listitem>

      <listitem>
       <para>
        Remove the <link
        linkend="catalog-pg-tablespace"><structname>spclocation</structname></link>
        field from <structname>pg_tablespace</structname> (Magnus Hagander)
       </para>

       <para>
        This field was duplicative of the symbolic links already
        present in the data directory.  This allows tablespace
        directories to be moved while the server is down.  Also add
        <function>pg_tablespace_location()</function> to allow querying of the
        symbolic links.
       </para>
      </listitem>

      <listitem>
       <para>
        Move <type>tsvector</type> most-common-element statistics to new
        <link linkend="view-pg-stats"><structname>pg_stats</structname></link> columns
        (Alexander Korotkov)
       </para>

       <para>
        Consult <structname>most_common_elems</structname>
        and <structname>most_common_elem_freqs</structname> for the data formerly
        available in <structname>most_common_vals</structname>
        and <structname>most_common_freqs</structname>.
       </para>
      </listitem>

    </itemizedlist>

    <sect4>
     <title>Temporal Functions</title>

     <itemizedlist>

      <listitem>
       <para>
        Have <link
        linkend="functions-datetime-extract"><function>EXTRACT</function></link> of
        a non-timezone-aware value measure the epoch from local midnight,
        not <acronym>UTC</acronym> midnight (Tom Lane)
       </para>

       <para>
        Having these computations depend on <acronym>UTC</acronym>
        was inconsistent.  The old behavior is available by casting the
        value to timestamp with time zone.
       </para>
      </listitem>

      <listitem>
       <para>
        Properly parse time strings with trailing <literal>yesterday</literal>,
        <literal>today</literal>, and <literal>tomorrow</literal> (Dean Rasheed)
       </para>

       <para>
        Previously, <command>SELECT '04:00:00
        yesterday'::timestamp</command> returned yesterday's date at
        midnight.
       </para>
      </listitem>

      <listitem>
       <para>
        Fix <link
        linkend="functions-formatting"><function>to_date()</function></link> and
        <function>to_timestamp()</function> to wrap incomplete dates toward 2020
        (Bruce Momjian)
       </para>

       <para>
        Previously, supplied years and year masks of less than four digits
        wrapped inconsistently.
       </para>
      </listitem>

     </itemizedlist>
    </sect4>

   </sect3>

   <sect3>
    <title>Object Modification</title>

    <itemizedlist>

      <listitem>
       <para>
        Prevent  <link linkend="sql-alterdomain"><command>ALTER
        DOMAIN</command></link> from working on non-domain types (Peter
        Eisentraut)
       </para>

       <para>
        Owner and schema changes were previously possible on non-domain
        types.
       </para>
      </listitem>

      <listitem>
       <para>
        No longer auto-lowercase procedural language names (Robert Haas)
       </para>

       <para>
        While unquoted language identifiers are still lowercased, strings
        and quoted identifiers are no longer forcibly down-cased.
        Thus for example <literal>CREATE FUNCTION ... LANGUAGE 'C'</literal>
        will no longer work; it must be spelled <literal>'c'</literal>, or better
        omit the quotes.
       </para>
      </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>Command-Line Tools</title>

    <itemizedlist>

      <listitem>
       <para>
        Provide consistent backquote, variable
        expansion, and quoted substring behavior in <link
        linkend="app-psql"><application>psql</application></link> meta-command
        arguments (Tom Lane)
       </para>

       <para>
        Previously, such usage was only properly expanded when isolated by
        whitespace, e.g.  <literal>'FOO'BAR</literal> was output as <literal>FOO
        BAR</literal>.
       </para>
      </listitem>

      <listitem>
       <para>
        No longer treat <link
        linkend="app-clusterdb"><application>clusterdb</application></link>
        table names as double-quoted; no longer treat <link
        linkend="app-reindexdb"><application>reindexdb</application></link> table
        and index names as double-quoted (Bruce Momjian)
       </para>

       <para>
        Users must supply double-quotes in the command arguments to
        accomplish double-quoting behavior.
       </para>
      </listitem>

      <listitem>
       <para>
        Disable prompting for the user name in <link
        linkend="app-dropdb"><application>dropdb</application></link> unless
        <option>--interactive</option> is specified (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        No longer have <link
        linkend="app-createuser"><application>createuser</application></link> prompt
        for settings (Peter Eisentraut)
       </para>

       <para>
        Previously <application>createuser</application> prompted regarding various
        user settings.
       </para>
      </listitem>

      <listitem>
       <para>
        Remove <link linkend="app-initdb"><application>initdb</application></link>
        support for the crypt authentication method (Peter Eisentraut)
       </para>

       <para>
        This was removed from the backend years ago.
       </para>
      </listitem>

    </itemizedlist>

   </sect3>

   <sect3>
    <title>Server Settings</title>

    <itemizedlist>
      <listitem>
       <para>
        Remove the <filename>postgresql.conf</filename> setting
        <varname>silent_mode</varname> (Heikki Linnakangas)
       </para>

       <para>
        This behavior could already be accomplished with <command>pg_ctl
        -l postmaster.log</command>.
       </para>
      </listitem>

      <listitem>
       <para>
        Remove <acronym>GUC</acronym> <varname>wal_sender_delay</varname>,
        as it is no longer needed (Tom Lane)
       </para>

       <para>
        The new "latch" capability made this setting unnecessary.
       </para>
      </listitem>

      <listitem>
       <para>
        Remove the <varname>custom_variable_classes</varname>
        <filename>postgresql.conf</filename> parameter (Tom Lane)
       </para>

       <para>
        The checking provided by this setting was dubious.  Now any
        setting can be prefixed by any class name.
       </para>
      </listitem>

      <listitem>
       <para>
        Add GUC <link linkend="guc-ssl-key-file">parameters</link> for
        specifying the locations of server-side <acronym>SSL</acronym>
        files (Peter Eisentraut)
       </para>

       <para>
        This allows changing the location of the files that were previously
        hard-coded to <filename>server.crt</filename>, <filename>server.key</filename>,
        <filename>root.crt</filename>, <filename>root.crl</filename>.  <acronym>CA</acronym>
        (crt) and <acronym>CRL</acronym> files no longer have default
        names, and if specified, these files must exist.
       </para>
      </listitem>

    </itemizedlist>
   </sect3>

   <sect3>
    <title>Monitoring</title>

    <itemizedlist>

      <listitem>
       <para>
        Rename <link
        linkend="monitoring-stats-views-table"><structname>pg_stat_activity</structname></link><structname>.procpid</structname>
        to <structname>pid</structname>, to match other system tables (Magnus Hagander)
       </para>
      </listitem>

      <listitem>
       <para>
        Create a separate <structname>pg_stat_activity</structname> column to
        report state information, e.g. idle (Scott Mead, Magnus Hagander)
       </para>

       <para>
        The last query values are preserved, allowing for enhanced analysis.
       </para>
      </listitem>

      <listitem>
       <para>
        Rename <structname>pg_stat_activity.current_query</structname> to
        <structname>query</structname> because it is not cleared when the query
        completes (Magnus Hagander)
       </para>
      </listitem>

      <listitem>
       <para>
        Change all <acronym>SQL</acronym>-level statistics timing values
        to float8-stored milliseconds (Tom Lane)
       </para>

       <para>
        The columns affected are:
        <structname>pg_stat_user_functions.total_time</structname>,
        <structname>pg_stat_user_functions.self_time</structname>,
        <structname>pg_stat_xact_user_functions.total_time</structname>,
        <structname>pg_stat_xact_user_functions.self_time</structname>.
       </para>
      </listitem>

      <listitem>
       <para>
        Change <application>pg_stat_statements</application>'
        <structname>total_time</structname> column to be measured in milliseconds
        (Tom Lane)
       </para>
      </listitem>

    </itemizedlist>

   </sect3>

  </sect2>

  <sect2>
   <title>Changes</title>

   <para>
    Below you will find a detailed account of the changes between
    <productname>PostgreSQL</productname> 9.2 and the previous major
    release.
   </para>

   <sect3>
    <title>Server</title>

    <sect4>
     <title>Performance</title>

     <itemizedlist>
      <listitem>
       <para>
        Allow queries to retrieve data only from indexes, avoiding heap
        access (Robert Haas, Ibrar Ahmed, Heikki Linnakangas, Tom Lane)
       </para>

       <para>
        This is often called "index-only scans", a feature which now enables
        the use of indexes with additional columns, or "covering indexes".
        This is possible for heap pages with exclusively all-visible
        tuples, as reported by the visibility map.  The visibility map was
        made crash-safe as a necessary part of implementing this feature.
       </para>
      </listitem>

      <listitem>
       <para>
        Allow frequently uncontended locks to be recorded using a new
        lightweight lock mechanism (Robert Haas)
       </para>
      </listitem>

      <listitem>
       <para>
        Move the frequently accessed members of the <structname>PGPROC</structname>
        shared memory array to a separate section, for performance (Pavan
        Deolasee, Heikki Linnakangas, Robert Haas)
       </para>
      </listitem>

      <listitem>
       <para>
        Reduce overhead of creating virtual transaction id locks (Robert
        Haas)
       </para>
      </listitem>

      <listitem>
       <para>
        Reduce overhead for shared invalidation cache messages (Robert
        Haas)
       </para>
      </listitem>

      <listitem>
       <para>
        Improve <command>COPY</command> performance by adding tuples to
        the heap in batches (Heikki Linnakangas)
       </para>
      </listitem>

      <listitem>
       <para>
        Improve GiST geometric type index performance by producing better
        trees with less memory allocation overhead (Alexander Korotkov)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow hint bits to be set sooner for temporary and unlogged tables
        (Robert Haas)
       </para>
      </listitem>

      <listitem>
       <para>
        Reduce checkpoints and <acronym>WAL</acronym> traffic on low
        activity servers (Simon Riggs)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow sorting to be performed by inlined and faster,
        non-<acronym>SQL</acronym>-callable comparison functions (Peter
        Geoghegan, Robert Haas, Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Add the <link linkend="SPGiST">SP-GiST</link> (Space-Partitioned
        GiST) index access method (Teodor Sigaev, Oleg Bartunov, Tom
        Lane, Robert Haas)
       </para>

       <para>
        SP-GiST is comparable to GiST in flexibility, but supports
        non-balanced partitioned search structures rather than balanced
        trees.  CLARIFY?
       </para>
      </listitem>

      <listitem>
       <para>
        Improve GiST index build times (Alexander Korotkov, Heikki
        Linnakangas)
       </para>
      </listitem>

      <listitem>
       <para>
        Take fewer <acronym>MVCC</acronym> snapshots, for performance
        (Robert Haas)
       </para>
      </listitem>

      <listitem>
       <para>
        Have the number of clog buffers scale based on <link
        linkend="guc-shared-buffers"><varname>shared_buffers</varname></link>
        (Robert Haas, Simon Riggs, Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Reduce the overhead of serializable isolation level locks (Dan
        Ports)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow group commit to work effectively under heavy load (Peter
        Geoghegan, Simon Riggs)
       </para>

       <para>
        Previously, batching of commits became ineffective as the write
        workload increased because of internal lock contention.
       </para>
      </listitem>

      <listitem>
       <para>
        Improve PowerPC and Itanium spinlock performance (Manabu Ori,
        Robert Haas, Tom Lane)
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>Process Management</title>

     <itemizedlist>

      <listitem>
       <para>
        Create a dedicated worker process to perform checkpoints (Simon
        Riggs)
       </para>

       <para>
        Formerly the background writer did dirty page writing and
        checkpointing.
       </para>
      </listitem>

      <listitem>
       <para>
        Improve asynchronous commit behavior by waking up sooner (Simon
        Riggs)
       </para>

       <para>
        Previously, only <link
        linkend="guc-wal-writer-delay"><varname>wal_writer_delay</varname></link>
        triggered <acronym>WAL</acronym> flushing to disk; now filling a
        <acronym>WAL</acronym> buffer also triggers <acronym>WAL</acronym>
        writes.
       </para>
      </listitem>

      <listitem>
       <para>
        Allow backends to detect postmaster death via a pipe read failure,
        rather than polling (Peter Geoghegan, Heikki Linnakangas, Tom Lane)
       </para>

       <para>
        The wait events are internally called "latches".
       </para>
      </listitem>

      <listitem>
       <para>
        Allow the <link linkend="pg-stat-bgwriter-view">bgwriter</link>,
        <link linkend="guc-wal-writer-delay">walwriter</link>, <link
        linkend="monitoring-stats">statistics collector</link>, archiver,
        and checkpointer to sleep more efficiently during periods of
        inactivity (Peter Geoghegan, Tom Lane)
       </para>

       <para>
        This reduces <acronym>CPU</acronym> wake-ups, which dramatically
        reduces power consumption on idle servers.
       </para>
      </listitem>

      <listitem>
       <para>
        Pass the safe number of file descriptors to Windows processes
        (Heikki Linnakangas)
       </para>

       <para>
        This allows Windows sessions to cache more open file descriptors.
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>Optimizer</title>

     <itemizedlist>

      <listitem>
       <para>
        Improve the ability of the planner to choose parameterized plans
        (Tom Lane)
       </para>

       <para>
        A prepared statement is now parsed, analyzed, and rewritten,
        but not necessarily planned.  When the prepared plan is executed
        with parameters, the planner might replan it for every constant,
        or it might execute a generic plan if its cost is close to that
        of a constant-specific plan. CLARIFY
       </para>
      </listitem>

      <listitem>
       <para>
        Allow the optimizer to use <literal>CHECK</literal> constraints defined
        on tables referenced in subqueries in planning decisions (Tom Lane)
       </para>

       <para>
        This is only enabled when <link
        linkend="guc-constraint-exclusion"><literal>constraint_exclusion
        = on</literal></link>.
       </para>
      </listitem>

      <listitem>
       <para>
        Allow <literal>indexedcol op ANY(ARRAY[...])</literal> conditions to be
        used in plain indexscans (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Improve heuristics for determining the type of unknown values by
        considering other data types involved in the operation (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow better matching of index columns with non-default operator
        qualifications (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Improve the planner's ability to use nested loops with inner
        index scans (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Support <function>MIN</function>/<function>MAX</function> index optimizations on
        boolean columns (Marti Raudsepp)
       </para>
      </listitem>

     </itemizedlist>

    <sect5>
     <title>Optimizer Statistics</title>

     <itemizedlist>
      <listitem>
       <para>
        Improve the ability to use statistics on columns referenced in
        subqueries (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Improve optimizer estimation for subqueries using
        <literal>DISTINCT</literal> (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Collect and use element-frequency statistics for arrays (Alexander
        Korotkov, Tom Lane)
       </para>

       <para>
        This patch improves selectivity estimation for the array &lt;@,
        &amp;&amp;, and @&gt; (containment and overlaps) array operators.
       </para>
      </listitem>

      <listitem>
       <para>
        Improve costing of partial indexes (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow analyze statistics to be collected for foreign tables
        (Etsuro Fujita)
       </para>
      </listitem>

     </itemizedlist>

    </sect5>

    </sect4>

    <sect4>
     <title>Authentication</title>

     <itemizedlist>

      <listitem>
       <para>
        Do not assume roles and <literal>samerole</literal> specified in <link
        linkend="auth-pg-hba-conf"><filename>pg_hba.conf</filename></link>
        include superusers (Andrew Dunstan)
       </para>

       <para>
        This makes it easier to use reject lines with group roles.
       </para>
      </listitem>

      <listitem>
       <para>
        Make superuser imply replication privilege (Noah Misch)
       </para>

       <para>
        This avoids the need to explicitly assign such privileges.
       </para>
      </listitem>

      <listitem>
       <para>
        Adjust <filename>pg_hba.conf</filename> processing to more
        consistently handle token parsing (Brendan Jurd, &Aacute;lvaro
        Herrera)
       </para>
      </listitem>

      <listitem>
       <para>
        Disallow empty <filename>pg_hba.conf</filename> files (Tom Lane)
       </para>

       <para>
        This was done to more quickly report misconfiguration.
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>Monitoring</title>

     <itemizedlist>

      <listitem>
       <para>
        Attempt to log the current query string before a backend crash
        (Marti Raudsepp)
       </para>
      </listitem>

      <listitem>
       <para>
        Make logging of autovacuum I/O activity more verbose (Greg
        Smith, Noah Misch)
       </para>

       <para>
        This logging is triggered by <link
        linkend="guc-log-autovacuum-min-duration"><varname>log_autovacuum_min_duration</varname></link>.
       </para>
      </listitem>

      <listitem>
       <para>
        Have <acronym>WAL</acronym> replay report failures sooner
        (Fujii Masao)
       </para>

       <para>
        There were some cases where failures were only reported once the
        server went into master mode.
       </para>
      </listitem>

      <listitem>
       <para>
        Add <link
        linkend="functions-admin-backup"><function>pg_xlog_location_diff()</function></link>
        to simplify xlog comparisons (Euler Taveira de Oliveira)
       </para>

       <para>
        This is useful for computing replication lag.
       </para>
      </listitem>

      <listitem>
       <para>
        Support configurable eventlog application names on Windows
        (MauMau, Magnus Hagander)
       </para>

       <para>
        This allows different instances to use the eventlog
        with different identifiers, by setting the <link
        linkend="guc-event-source"><varname>event_source</varname></link>
        <acronym>GUC</acronym>, which is similar to how <link
        linkend="guc-syslog-ident"><varname>syslog_ident</varname></link> works.
       </para>
      </listitem>

      <listitem>
       <para>
        Change "unexpected EOF" message to <literal>DEBUG1</literal> level
        (Magnus Hagander)
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>Statistical Views</title>

     <itemizedlist>

      <listitem>
       <para>
        Track temporary file sizes and file counts in the <link
        linkend="monitoring-stats-views"><structname>pg_stat_database</structname></link>
        system view (Tomas Vondra)
       </para>
      </listitem>

      <listitem>
       <para>
        Add a deadlock counter to the <structname>pg_stat_database</structname>
        system view (Magnus Hagander)
       </para>
      </listitem>

      <listitem>
       <para>
        Add a <acronym>GUC</acronym> setting <link
        linkend="guc-track-io-timing"><varname>track_io_timing</varname></link>
        to track I/O timings (Ants Aasma, Robert Haas)
       </para>
      </listitem>

      <listitem>
       <para>
        Report checkpoint timing information in <link
        linkend="pg-stat-bgwriter-view"><structname>pg_stat_bgwriter</structname></link>
        (Greg Smith, Peter Geoghegan, Robert Haas)
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>Server Settings</title>

     <itemizedlist>

      <listitem>
       <para>
        Allow <link
        linkend="guc-deadlock-timeout"><varname>deadlock_timeout</varname></link>
        to be set per-session by superusers, not just per-cluster (Noah
        Misch)
       </para>

       <para>
        This allows <varname>deadlock_timeout</varname> to be reduced for
        transactions that are likely to be involved in a deadlock,
        thus detecting it more quickly. It can also be used to reduce
        the chances of a session being chosen for cancellation due to
        a deadlock.
       </para>
      </listitem>

      <listitem>
       <para>
        Add <acronym>GUC</acronym> parameter <link
        linkend="guc-temp-file-limit"><varname>temp_file_limit</varname></link>
        to constrain temporary file space usage per session (Mark Kirkwood)
       </para>
      </listitem>

      <listitem>
       <para>
        Add <filename>postgresql.conf</filename> category "Replication /
        Sending Servers" (Fujii Masao)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow a superuser to <command>SET</command> an extension's
        superuser-only custom variable before loading the associated
        extension (Tom Lane)
       </para>

       <para>
        The system now remembers whether a <command>SET</command> was
        performed by a superuser.
       </para>
      </listitem>

      <listitem>
       <para>
        Add <link linkend="app-postmaster">postmaster</link> <option>-C</option>
        option to query configuration parameters (Bruce Momjian)
       </para>

       <para>
        This allows <application>pg_ctl</application> to better handle
        configuration-only directory installations.
       </para>
      </listitem>

      <listitem>
       <para>
        Force the locale to be recorded more specifically, rather than
        using the empty string to mean the default (Tom Lane)
       </para>
      </listitem>

     </itemizedlist>

     <sect5>
      <title><filename>postgresql.conf</filename></title>

      <itemizedlist>

      <listitem>
       <para>
        Allow multiple errors in <filename>postgresql.conf</filename>
        to be reported, rather than just the first one (Alexey Klyukin,
        Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow a reload of <filename>postgresql.conf</filename> to be
        processed by all backends even if there are some settings that
        are invalid for that session (Alexey Klyukin)
       </para>

       <para>
        Previously, such not-valid-for-session errors would cause all
        setting changes to be ignored by that backend.
       </para>
      </listitem>

      <listitem>
       <para>
        Add an <literal>include_if_exists</literal> facility for configuration
        files (Greg Smith)
       </para>

       <para>
        This works the same as <literal>include</literal>, except that an error
        is not thrown if the file is missing.
       </para>
      </listitem>

      <listitem>
       <para>
        Check the server time zone during initdb, and set
        <filename>postgresql.conf</filename> values
        <link linkend="guc-timezone"><varname>timezone</varname></link> and
        <link linkend="guc-log-timezone"><varname>log_timezone</varname></link>
        accordingly (Tom Lane)
       </para>

       <para>
        This avoids expensive time zone probes during server start.
       </para>
      </listitem>

      <listitem>
       <para>
        Allow Windows to report
        <filename>postgresql.conf</filename> line numbers in <link
        linkend="view-pg-settings"><structname>pg_settings</structname></link>
        (Tom Lane)
       </para>
      </listitem>

      </itemizedlist>

     </sect5>

    </sect4>

   </sect3>

   <sect3>
    <title>Replication and Recovery</title>

   <sect4>
    <title>Streaming Replication</title>

    <itemizedlist>

      <listitem>
       <para>
        Allow streaming replications slaves to serve other slaves (Fujii
        Masao)
       </para>

       <para>
        Previously, only the master server could supply streaming
        replication log files to standby servers.  Slaves can also now
        do continuous archiving. (?)
       </para>
      </listitem>

      <listitem>
       <para>
        Add new <link
        linkend="guc-synchronous-commit"><varname>synchronous_commit</varname></link>
        replication mode <literal>remote_write</literal> (Fujii Masao, Simon Riggs)
       </para>

       <para>
        This only writes data to the socket, but does not wait a write
        to the remote disk.
       </para>
      </listitem>

      <listitem>
       <para>
        Add a <link
        linkend="app-pgreceivexlog"><application>pg_receivexlog</application></link>
        tool to archive xlog file changes as they are written, rather
        than waiting for completed xlog files (Magnus Hagander)
       </para>
      </listitem>

      <listitem>
       <para>
        Send keepalive messages to standby servers (Simon Riggs)
       </para>

       <para>
        This allows administrators to accurately calculate streaming
        replication transfer and application latency.
       </para>
      </listitem>

     </itemizedlist>

     <sect5>
      <title><link linkend="app-pgbasebackup"><application>pg_basebackup</application></link></title>

      <itemizedlist>

      <listitem>
       <para>
        Allow streaming of xlog files while <application>pg_basebackup</application>
        is performing a backup (Magnus Hagander)
       </para>

       <para>
        This allows passing of xlog files to the standby before they are
        discarded on the primary.
       </para>
      </listitem>

      <listitem>
       <para>
        Allow <application>pg_basebackup</application> to pull from standby servers
        (Jun Ishizuka, Fujii Masao)
       </para>
      </listitem>

      <listitem>
       <para>
        Exclude the postmaster.opts file from <application>pg_basebackup</application>
        backups (Magnus Hagander)
       </para>
      </listitem>

      </itemizedlist>

     </sect5>
    </sect4>

   </sect3>

   <sect3>
    <title>Queries</title>

     <itemizedlist>

      <listitem>
       <para>
        Improve columns labels produced by subqueries (Marti Raudsepp)
       </para>

       <para>
        Previously, the generic label <literal>?column?</literal> was used.
       </para>
      </listitem>

      <listitem>
       <para>
        Preserve column names in row expressions (Andrew Dunstan, Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Cancel queries if clients get disconnected (Florian Pflug)
       </para>
      </listitem>

      <listitem>
       <para>
        Silently ignore nonexistent schemas specified in <link
        linkend="guc-search-path"><varname>search_path</varname></link> (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Warn about casts to and from domain types (Robert Haas)
       </para>

       <para>
        Such casts have no effect.
       </para>
      </listitem>

      <listitem>
       <para>
        When a row fails a <literal>CHECK</literal> or <literal>NOT NULL</literal>
        constraint, show the row's contents in errdetail (Jan
        Kundr&aacute;t)
       </para>

       <para>
        This should make it easier to identify which row is problematic
        when an insert or update is processing many rows.
       </para>
      </listitem>

     </itemizedlist>

   </sect3>

   <sect3>
    <title>Object Manipulation</title>

     <itemizedlist>

      <listitem>
       <para>
        Provide more reliable operation during concurrent
        <acronym>DDL</acronym> (Robert Haas, Noah Misch)
       </para>
      </listitem>

      <listitem>
       <para>
        Add <literal>CONCURRENTLY</literal> option to <link
        linkend="sql-dropindex"><command>DROP INDEX
        CONCURRENTLY</command></link> (Simon Riggs)
       </para>

       <para>
        This allows index removal without blocking other sessions.
       </para>
      </listitem>

     </itemizedlist>

    <sect4>
     <title>Constraints</title>

     <itemizedlist>

      <listitem>
       <para>
        Allow <link linkend="ddl-constraints"><literal>CHECK</literal></link>
        constraints to be declared <literal>NOT VALID</literal> (&Aacute;lvaro
        Herrera)
       </para>

       <para>
        A <literal>NOT VALID</literal> <literal>CHECK</literal> constraint can be added
        to a table without the table's data being verified against the
        constraint.  New rows are verified.
       </para>

       <para>
        Such constraints are ignored by the planner when considering
        <varname>constraint_exclusion</varname>.  <command>ALTER TABLE</command> /
        <literal>VALIDATE</literal> allows <literal>NOT VALID</literal> <literal>CHECK</literal>
        check constraints to be fully validated and considered for
        <varname>constraint_exclusion</varname>, including re-planning of
        cached plans.
       </para>
      </listitem>

      <listitem>
       <para>
        Allow <literal>CHECK</literal> constraints to be declared <literal>NO
        INHERIT</literal> (Nikhil Sontakke, Alex Hunsaker)
       </para>

       <para>
        This makes them enforceable only on the parent table, not on
        child tables.
       </para>
      </listitem>

      <listitem>
       <para>
        Add the ability to <link linkend="sql-altertable">rename</link>
        constraints (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Disallow merging of <literal>ONLY</literal> and non-<literal>ONLY</literal>
        constraints in child tables (Nikhil Sontakke)
       </para>

       <para>
        Such merging would cause unpredictable behavior in grandchild
        tables.
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><command>ALTER</command></title>

     <itemizedlist>

      <listitem>
       <para>
        Reduce need to rebuild tables and indexes for various <link
        linkend="sql-altertable"><command>ALTER TABLE</command></link>
        operations (Noah Misch) DUPLICATE?
       </para>
      </listitem>

      <listitem>
       <para>
        Add <literal>IF EXIST</literal> clause to <command>ALTER</command>
        commands (Pavel Stehule)
       </para>

       <para>
        For example, <command>ALTER FOREIGN TABLE IF EXISTS foo RENAME
        TO bar</command>.
       </para>
      </listitem>

      <listitem>
       <para>
        Add <link linkend="sql-alterforeigndatawrapper"><command>ALTER
        FOREIGN DATA WRAPPER</command></link> / <literal>RENAME</literal>
        and <link linkend="sql-alterserver"><command>ALTER
        SERVER</command></link> / <literal>RENAME</literal> (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Add <link linkend="sql-alterdomain"><command>ALTER
        DOMAIN</command></link> / <literal>RENAME</literal> (Peter Eisentraut)
       </para>

       <para>
        You could already rename domains using <command>ALTER
        TYPE</command>.
       </para>
      </listitem>

      <listitem>
       <para>
        Throw an error for <command>ALTER DOMAIN</command> / <literal>DROP
        CONSTRAINT</literal> on a nonexistent constraint (Peter Eisentraut)
       </para>

       <para>
        An <literal>IF EXISTS</literal> option has been added to enable the
        previous behavior.
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><link linkend="sql-createtable"><command>CREATE/ALTER
     TABLE</command></link></title>

     <itemizedlist>

      <listitem>
       <para>
        Fix <command>CREATE TABLE</command> ... <literal>AS EXECUTE</literal>
        to handle <literal>WITH NO DATA</literal> and column name specification
        (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Support <command>CREATE TABLE (LIKE ...)</command> from foreign
        tables and views (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow <command>CREATE TABLE (LIKE ...)</command> from composite
        type (Peter Eisentraut)
       </para>

       <para>
        This also allows a table to be created whose schema matches a view.
       </para>
      </listitem>

      <listitem>
       <para>
        Tighten creation of tables in temporary schemas (Robert Haas,
        Amit Khandekar)
       </para>

       <para>
        Previously, it was possible to create unlogged tables in temporary
        schemas, and temporary tables in the temporary schemas of other
        sessions.
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>Object Permissions</title>

     <itemizedlist>

      <listitem>
       <para>
        Add a <link
        linkend="sql-createview"><literal>security_barrier</literal></link>
        option for views (KaiGai Kohei)
       </para>

       <para>
        This prevents <literal>security_barrier</literal> views from being moved
        into other scopes, preventing possible leakage of view-prohibited
        data.  Such views might perform more poorly.
       </para>
      </listitem>

      <listitem>
       <para>
        Add a new <link
        linkend="sql-createfunction"><literal>LEAKPROOF</literal></link> function
        specification to mark functions that can be safely pushed down
        into <literal>security_barrier</literal> views (KaiGai Kohei)
       </para>
      </listitem>

      <listitem>
       <para>
        Add support for privileges on date types (Peter Eisentraut)
       </para>

       <para>
        This adds support for the <acronym>SQL</acronym>-conforming
        <literal>USAGE</literal> privilege on types and domains.  The intent is
        to be able restrict which users can create dependencies on types,
        which restricts the way in which owners can alter types.
       </para>
      </listitem>

      <listitem>
       <para>
        Check for <command>INSERT</command> privileges in <command>SELECT
        INTO</command> / <command>CREATE TABLE AS</command> (KaiGai Kohei)
       </para>

       <para>
        Because the object is being created by <command>SELECT
        INTO</command> or <command>CREATE TABLE AS</command>, it will
        have default insert permissions, except if <literal>ALTER DEFAULT
        PRIVILEGES</literal> has removed such permissions, hence the need for
        a permission check.
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

   <sect3>
    <title>Utility Operations</title>

    <itemizedlist>

      <listitem>
       <para>
        Allow vacuum to more easily skip pages that cannot be locked
        (Simon Riggs, Robert Haas)
       </para>

       <para>
        This change should greatly reduce the incidence of vacuum getting
        "stuck".
       </para>
      </listitem>

    </itemizedlist>

    <sect4>
     <title><link linkend="sql-explain"><command>EXPLAIN</command></link></title>

      <itemizedlist>

      <listitem>
       <para>
        Make <command>EXPLAIN (BUFFERS)</command> track blocks dirtied
        and written (Robert Haas)
       </para>
      </listitem>

      <listitem>
       <para>
        Have <command>EXPLAIN ANALYZE</command> report the number of rows
        rejected by filter steps (Marko Tiikkaja)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow <command>EXPLAIN</command> to avoid timing overhead when
        time values are unwanted (Tomas Vondra)
       </para>

       <para>
        This is accomplished by setting <literal>TIMING</literal> to
        <literal>FALSE</literal>.
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

   <sect3>
    <title>Data Types</title>

    <itemizedlist>

      <listitem>
       <para>
        Add a <link linkend="datatype-json"><type>JSON</type></link>
        data type (Robert Haas)  AUTHOR?
       </para>

       <para>
        This stores <acronym>JSON</acronym> data as text after proper
        validation.
       </para>
      </listitem>

      <listitem>
       <para>
        Add <link
        linkend="functions-json"><function>array_to_json()</function></link>
        and <function>row_to_json()</function> (Andrew Dunstan)
       </para>
      </listitem>

      <listitem>
       <para>
        Add support for <link linkend="rangetypes">range data types</link>
        (Jeff Davis, Tom Lane, Alexander Korotkov)
       </para>

       <para>
        The range data type records a lower and upper bound, and supports
        operations like contains, overlaps, and intersection.
       </para>
      </listitem>

      <listitem>
       <para>
        Add <link linkend="datatype-serial"><type>SMALLSERIAL</type></link>
        data type (Mike Pultz)
       </para>

       <para>
        This is like <type>SERIAL</type>, except it stores the sequence in
        a two-byte integer column (int2).
       </para>
      </listitem>

      <listitem>
       <para>
        Allow <link linkend="sql-createdomain">domains</link> to be
        declared <literal>NOT VALID</literal> (&Aacute;lvaro Herrera)
       </para>

       <para>
        This can be set at domain creation time, or via
        <command>ALTER DOMAIN</command> / <literal>ADD CONSTRAINT / NOT
        VALID</literal>. <command>ALTER DOMAIN</command> / <literal>VALIDATE
        CONSTRAINT</literal> fully validates the domain.
       </para>
      </listitem>

      <listitem>
       <para>
        Support more locale-specific formatting options for the <link
        linkend="datatype-money">MONEY</link> data type (Tom Lane)
       </para>

       <para>
        Specifically, display the plus/minus sign, currency symbol,
        and separators in a locale-specific order.  CORRECT?
       </para>
      </listitem>

      <listitem>
       <para>
        Add bitwise "and", "or", and "not" <link
        linkend="macaddr-functions-table">operators</link> for the macaddr
        data type (Brendan Jurd)
       </para>
      </listitem>

    </itemizedlist>

    <sect4>
     <title><link linkend="functions-xml"><acronym>XML</acronym></link></title>

     <itemizedlist>

      <listitem>
       <para>
        Allow <function>xpath()</function> to return a single-element
        <acronym>XML</acronym> array when supplied a scalar value (Florian
        Pflug)
       </para>

       <para>
        Previously, it returned an empty array.  This change will also
        cause <function>xpath_exists()</function> to return true, not false,
        for such expressions.
       </para>
      </listitem>

      <listitem>
       <para>
        Improve <acronym>XML</acronym> error handling to be more robust
        (Florian Pflug)
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

   <sect3>
    <title>Functions</title>

     <itemizedlist>

      <listitem>
       <para>
        Allow users to cancel queries in user-matching sessions using <link
        linkend="functions-admin-signal"><function>pg_cancel_backend()</function></link>
        (Magnus Hagander, Josh Kupershmidt)
       </para>

       <para>
        Previously only the superuser could cancel queries.
       </para>
      </listitem>

      <listitem>
       <para>
        Allow <link
        linkend="functions-aggregate-table"><function>string_agg()</function></link>
        to process bytea values (Pavel Stehule)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow regular expressions to use back-references (Tom Lane)
       </para>

       <para>
        For example, '^(\w+)( \1)+$'.
       </para>
      </listitem>

      <listitem>
       <para>
        Allow importing and exporting of transaction snapshots (Joachim
        Wieland, Tom Lane)
       </para>

       <para>
        Snapshots are exported via <link
        linkend="functions-snapshot-synchronization"><function>pg_export_snapshot()</function></link>,
        and imported via <command>SET TRANSACTION SNAPSHOT</command>.
        Only snapshots of currently-running transactions can be imported.
       </para>
      </listitem>

      <listitem>
       <para>
        Support <link
        linkend="functions-info-catalog-table"><literal>COLLATION
        FOR</literal></link> on expressions (Peter Eisentraut)
       </para>

       <para>
        This returns a string representing the collation of the expression.
       </para>
      </listitem>

      <listitem>
       <para>
        Add a <type>NUMERIC</type> variant of <link
        linkend="functions-admin-dbsize"><function>pg_size_pretty()</function></link>
        for use with <function>pg_xlog_location_diff()</function> (Fujii Masao)
       </para>
      </listitem>

     </itemizedlist>

    <sect4>
     <title>Function and Trigger Creation</title>

     <itemizedlist>

      <listitem>
       <para>
        Change default names of triggers to fire "action" triggers before
        "check" triggers (Tom Lane)
       </para>

       <para>
        This allows default-named check triggers to check post-action rows.
       </para>
      </listitem>

      <listitem>
       <para>
        Add a <link
        linkend="functions-info-session-table"><function>pg_trigger_depth()</function></link>
        function (Kevin Grittner)
       </para>

       <para>
        This reports the current trigger call depth.
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>Object Information Functions</title>

     <itemizedlist>
      <listitem>
       <para>
        Add <link
        linkend="functions-info-schema-table"><function>pg_opfamily_is_visible()</function></link>
        (Josh Kupershmidt)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow per-column foreign data wrapper options and
        <literal>force_not_null</literal> (Shigeru Hanada)
       </para>

       <para>
        This allows <acronym>SQL</acronym> specification of per-column
        information useful to foreign data sources.  Includes
        <application>psql</application> option display support.
       </para>
      </listitem>

      <listitem>
       <para>
        Improve pretty printing of view definitions (Andrew Dunstan)
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><link linkend="information-schema">Information Schema</link></title>

     <itemizedlist>

      <listitem>
       <para>
        Add <structname>information_schema</structname> views
        <structname>role_udt_grants</structname>, <structname>udt_privileges</structname>,
        and <structname>user_defined_types</structname> (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Add composite-type attributes to the
        <varname>information_schema</varname>.<varname>element_types</varname> view
        (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Implement information schema <varname>interval_type</varname> columns
        (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Add collation columns to various information schema views (Peter
        Eisentraut)
       </para>

       <para>
        Specifically, provide collation columns for views referencing
        attributes, columns, domains, and <varname>element_types</varname>.
       </para>
      </listitem>

      <listitem>
       <para>
        Add display of sequence <literal>USAGE</literal> privileges to information
        schema (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Add a <varname>with_hierarchy</varname> column to the information schema
        privileges display (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Have the information schema show default privileges (Peter
        Eisentraut)
       </para>

       <para>
        Previously, default permissions generated <literal>NULL</literal> fields.
        (WAS IT NULL?)
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

   <sect3>
    <title>Server-Side Languages</title>

    <sect4>
     <title><link linkend="plpgsql">PL/pgSQL</link> Server-Side Language</title>

     <itemizedlist>

      <listitem>
       <para>
        Allow the PL/pgSQL <command>OPEN</command> cursor command to supply
        parameters by name (Yeb Havinga)
       </para>
      </listitem>

      <listitem>
       <para>
        Add a <command>GET STACKED DIAGNOSTICS</command> PL/pgSQL command
        to retrieve exception info (Pavel Stehule)
       </para>
      </listitem>

      <listitem>
       <para>
        Speed up PL/pgSQL array assignment by caching type information
        (Pavel Stehule)
       </para>
      </listitem>

      <listitem>
       <para>
        Improve performance and memory consumption of the PL/pgSQL
        <literal>ELSIF</literal> clauses (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Output the function signature, not just the name, in PL/pgSQL
        error messages (Pavel Stehule)
       </para>
      </listitem>

    </itemizedlist>

    </sect4>

    <sect4>
     <title><link linkend="plpython">PL/Python</link> Server-Side Language</title>

     <itemizedlist>

      <listitem>
       <para>
        Add PL/Python <acronym>SPI</acronym> cursor support (Jan
        Urbanski)
       </para>

       <para>
        This allows PL/Python to read partial result sets.
       </para>
      </listitem>

      <listitem>
       <para>
        Add result metadata functions to PL/Python (Peter Eisentraut)
       </para>

       <para>
        Specifically, this adds result object functions .colnames,
        .coltypes, and .coltypmods.
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><link linkend="xfunc-sql">SQL</link> Server-Side Language</title>

     <itemizedlist>
      <listitem>
       <para>
        Allow <acronym>SQL</acronym>-language functions to reference
        parameters by name (Matthew Draper)
       </para>

       <para>
        To use this, simply name the function arguments and reference
        the argument names in the <acronym>SQL</acronym> function body.
       </para>
      </listitem>

     </itemizedlist>
    </sect4>

   </sect3>

   <sect3>
    <title>Client Applications</title>

    <itemizedlist>

      <listitem>
       <para>
        Add <link linkend="app-initdb"><application>initdb</application></link>
        options <option>--auth-local</option> and <option>--auth-host</option>
        (Peter Eisentraut)
       </para>

       <para>
        This allows individual control of <literal>local</literal> and
        <literal>host</literal> <filename>pg_hba.conf</filename> authentication
        settings.  <option>--auth</option> still controls both.
       </para>
      </listitem>

      <listitem>
       <para>
        Add <option>--replication</option>/<option>--no-replication</option> flags to
        <link linkend="app-createuser"><application>createuser</application></link>
        to control replication permission (Fujii Masao)
       </para>
      </listitem>

      <listitem>
       <para>
        Add the <option>--if-exists</option> option to <link
        linkend="app-dropdb"><application>dropdb</application></link> and <link
        linkend="app-dropuser"><application>dropuser</application></link> (Josh
        Kupershmidt)
       </para>
      </listitem>

      <listitem>
       <para>
        Give command-line tools the ability to specify the name of the
        database to connect to, and fall back to <varname>template1</varname>
        if a <literal>postgres</literal> database connection fails (Robert Haas)
       </para>
      </listitem>

    </itemizedlist>

    <sect4>
     <title><link linkend="app-psql"><application>psql</application></link></title>

     <itemizedlist>

      <listitem>
       <para>
        Add <application>psql</application> mode to auto-expand output based on the
        display width (Peter Eisentraut)
       </para>

       <para>
        This adds the <literal>auto</literal> option to the <command>\x</command>
        command, which switches to the expanded mode when the normal
        output would be wider than the screen.
       </para>
      </listitem>

      <listitem>
       <para>
        Allow inclusion of <application>psql</application> files relative to the
        directory of the input file from which it was invoked (Gurjeet
        Singh)
       </para>

       <para>
        The <application>psql</application> command is <command>\ir</command>.
       </para>
      </listitem>

      <listitem>
       <para>
        Add support for non-<acronym>ASCII</acronym> characters in
        <application>psql</application> variable names (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Add support for major-version-specific <filename>.psqlrc</filename> files
        (Bruce Momjian)
       </para>

       <para>
        <application>psql</application> already supported minor-version-specific
        <filename>.psqlrc</filename>
        files.
       </para>
      </listitem>

      <listitem>
       <para>
        Provide environment variable overrides for <application>psql</application>
        history and startup file locations (Andrew Dunstan)
       </para>

       <para>
        Specifically, <envar>PSQL_HISTORY</envar> and
        <envar>PSQLRC</envar>.
       </para>
      </listitem>

      <listitem>
       <para>
        Add a <application>psql</application> <command>\setenv</command> command to modify
        the environment variables used by child processes (Andrew Dunstan)
       </para>

       <para>
        This is useful in <filename>.psqlrc</filename> files.
       </para>
      </listitem>

      <listitem>
       <para>
        Have <application>psql</application> temporary editor files use a
        <filename>.sql</filename> extension (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Allows <application>psql</application> to use zero-byte field and record
        separators (Peter Eisentraut)
       </para>

       <para>
        Various shell tools use zero/null-byte separators, e.g. find.
       </para>
      </listitem>

      <listitem>
       <para>
        Have <application>psql</application> <command>\timing</command> report times for
        failed queries (Magnus Hagander)
       </para>

       <para>
        Previously only successful query times were reported.
       </para>
      </listitem>

      <listitem>
       <para>
        Unify and tighten <application>psql</application> treatment of <command>\copy</command>
        and plain <command>COPY</command> (Noah Misch)
       </para>

       <para>
        This makes failure behavior more predictable and honors <command>\set
        ON_ERROR_ROLLBACK</command>.
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title>Informational Commands</title>

     <itemizedlist>
      <listitem>
       <para>
        Make <application>psql</application> <command>\d</command> on a sequence show the
        table/column name owning it (Magnus Hagander)
       </para>
      </listitem>

      <listitem>
       <para>
        Show statistics target for columns in <application>psql</application>
        <command>\d+</command> (Magnus Hagander)
       </para>
      </listitem>

      <listitem>
       <para>
        Show role password expiration dates in <application>psql</application>
        <command>\du</command> (Fabr&iacute;zio de Royes Mello)
       </para>
      </listitem>

     </itemizedlist>


    <sect5>
     <title>Comments</title>

     <itemizedlist>
      <listitem>
       <para>
        Allow <application>psql</application> to display comments for languages and
        casts (Josh Kupershmidt)
       </para>

       <para>
        These are output via <command>\dL</command> and <command>\dC</command>.
       </para>
      </listitem>

      <listitem>
       <para>
        Allow <application>psql</application> to display comments for conversions
        and domains (Josh Kupershmidt)
       </para>

       <para>
        This is accomplished with the addition of <literal>+</literal> to
        <command>\dc</command> and <command>\dD</command> commands.
       </para>
      </listitem>

      <listitem>
       <para>
        Allow <application>psql</application> to display the comments on
        <acronym>SQL</acronym>/<acronym>MED</acronym> objects in verbose
        mode (Josh Kupershmidt)
       </para>
      </listitem>

      <listitem>
       <para>
        Change <application>psql</application>'s <command>\dd</command> command to display
        only objects without their own backslash command (Josh Kupershmidt)
       </para>
      </listitem>

     </itemizedlist>

    </sect5>

    </sect4>

    <sect4>
     <title>Tab Completion</title>

     <itemizedlist>

      <listitem>
       <para>
        In <application>psql</application> tab completion,
        complete <acronym>SQL</acronym> keywords based on
        <varname>COMP_KEYWORD_CASE</varname> setting and the perhaps case of
        the partially-supplied word (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Add <application>psql</application> tab completion support for
        <command>EXECUTE</command> (Andreas Karlsson)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow tab completion of role references in
        <command>GRANT</command>/<command>REVOKE</command> (Peter
        Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow tab completion of file names to supply quotes, when necessary
        (Noah Misch)
       </para>
      </listitem>

      <listitem>
       <para>
        Change <application>psql</application> tab completion support for
        <command>TABLE</command> to also display views (Magnus Hagander)
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

    <sect4>
     <title><link linkend="app-pgdump"><application>pg_dump</application></link></title>

     <itemizedlist>

      <listitem>
       <para>
        Add an <option>--exclude-table-data</option> option to
        <application>pg_dump</application> (Andrew Dunstan)
       </para>

       <para>
        This allows dumping of a table's definition but not its data,
        on a per-table basis.
       </para>
      </listitem>

      <listitem>
       <para>
        Add a <option>--section</option> option to <application>pg_dump</application>
        and <application>pg_restore</application> (Andrew Dunstan)
       </para>

       <para>
        Valid values are <literal>pre-data</literal>, <literal>data</literal>,
        and <literal>post-data</literal>. The option can be
        given more than once. <option>--schema-only</option>
        is equivalent to <literal>--section=pre-data
        --section=post-data</literal>. <option>--data-only</option> is equivalent to
        <literal>--section=data</literal>.
       </para>
      </listitem>

      <listitem>
       <para>
        Have <link
        linkend="app-pg-dumpall"><application>pg_dumpall</application></link> dump all
        roles first, then all configuration settings on roles (Phil Sorber)
       </para>

       <para>
        This allows a role's configuration settings to rely on other
        roles without generating an error.
       </para>
      </listitem>

      <listitem>
       <para>
        Allow <application>pg_dumpall</application> to avoid errors if the
        <literal>postgres</literal> database is missing in the new cluster
        (Robert Haas)
       </para>
      </listitem>

      <listitem>
       <para>
        Dump foreign server user mappings in user name order (Peter
        Eisentraut)
       </para>

       <para>
        This helps produce deterministic dump files.
       </para>
      </listitem>

      <listitem>
       <para>
        Dump operators in a predictable order (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Tighten rules for when extension configuration tables are dumped
        by <application>pg_dump</application> (Tom Lane)
       </para>
      </listitem>

     </itemizedlist>

    </sect4>

   </sect3>

    <sect3>
     <title><link linkend="libpq"><application>libpq</application></link></title>

     <itemizedlist>

      <listitem>
       <para>
        Add <acronym>URI</acronym>s support to libpq connection functions
        (Alexander Shulgin)
       </para>

       <para>
        The syntax begins with <literal>postgres://</literal>.
       </para>
      </listitem>

      <listitem>
       <para>
        Add a libpq <link linkend="libpq-connect-sslcompression">connection
        option</link> to disable <acronym>SSL</acronym> compression
        (Laurenz Albe)
       </para>

       <para>
        This can be used to remove the overhead of <acronym>SSL</acronym>
        compression on fast networks.
       </para>
      </listitem>

      <listitem>
       <para>
        Add a row processor <acronym>API</acronym> to libpq for better
        handling of large result sets (Kyotaro Horiguchi)
       </para>

       <para>
        Previously, libpq collected the entire query result into memory
        before passing it back to the application.
       </para>
      </listitem>

      <listitem>
       <para>
        Enhance <literal>const</literal> qualifiers in the libpq functions
        PQconnectdbParams, PQconnectStartParams, and PQpingParams (Lionel
        Elie Mamane)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow libpq's <filename>.pgpass</filename> file to process escaped characters
        in the password field (Robert Haas)
       </para>
      </listitem>

      <listitem>
       <para>
        Have library functions use <function>abort()</function> instead of
        <function>exit()</function> to terminate the executable (Peter Eisentraut)
       </para>

       <para>
        This does not interfere with the normal exit codes used by the
        program, and generates a signal that can be caught by the caller.
       </para>
      </listitem>

      <listitem>
       <para>
        Fix a libpq memory leak by freeing client encoding connection
        parameters (Peter Eisentraut)
       </para>
      </listitem>

     </itemizedlist>

   </sect3>

   <sect3>
    <title>Source Code</title>

     <itemizedlist>

      <listitem>
       <para>
        Add support for building with <link linkend="install-windows">MS
        Visual Studio 2010</link> (Brar Piening)
       </para>
      </listitem>

      <listitem>
       <para>
        Enable compiling with the MinGW-w64 32-bit compiler (Lars Kanis)
       </para>
      </listitem>

      <listitem>
       <para>
        Install <filename>plpgsql.h</filename> into <filename>include/server</filename> during installation
        (Heikki Linnakangas)
       </para>
      </listitem>

      <listitem>
       <para>
        Use C flexible array members, where supported (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Improve the concurrent transaction regression tests
        (isolationtester) (Noah Misch)
       </para>
      </listitem>

      <listitem>
       <para>
        Modify <application>thread_test</application> to create its test files in
        the current directory, rather than /tmp (Bruce Momjian)
       </para>
      </listitem>

      <listitem>
       <para>
        Improve flex and bison warning and error reporting (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Add memory barrier support (Robert Haas)
       </para>

       <para>
        This is currently unused.
       </para>
      </listitem>

      <listitem>
       <para>
        Modify pgindent to use a typedef file (Bruce Momjian)
       </para>
      </listitem>

      <listitem>
       <para>
        Add a hook for processing messages due to be sent to the server
        log (Martin Pihlak)
       </para>
      </listitem>

      <listitem>
       <para>
        Add object access hooks for <command>DROP</command> commands
        (KaiGai Kohei)
       </para>
      </listitem>

      <listitem>
       <para>
        Centralize <command>DROP</command> handling for some object types
        (KaiGai Kohei)
       </para>
      </listitem>

      <listitem>
       <para>
        Add a <application>pg_upgrade</application> test suite (Peter Eisentraut)
       </para>
      </listitem>

      <listitem>
       <para>
        Sync regular expression code with <acronym>TCL</acronym> 8.5.11
        and improve internal processing (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Move <acronym>CRC</acronym> tables to libpgport, and provide them
        in a separate include file (Daniel Farina)
       </para>
      </listitem>

      <listitem>
       <para>
        Add options to <application>git_changelog</application> for use in major
        release note creation (Bruce Momjian)
       </para>
      </listitem>

      <listitem>
       <para>
        Remove dead ports (Peter Eisentraut)
       </para>

       <para>
        The removed ports are: dgux, nextstep, sunos4, svr4, ultrix4,
        univel, bsdi.
       </para>
      </listitem>

     </itemizedlist>

   </sect3>

   <sect3>
    <title>Modules</title>

    <itemizedlist>

      <listitem>
       <para>
        Improve efficiency of <link linkend="dblink">dblink</link> by
        using libpq's new row processor <acronym>API</acronym> (Kyotaro
        Horiguchi)
       </para>

       <para>
        This cannot be used by
        <function>dblink_send_query()</function>/<function>dblink_get_result()</function>.
       </para>
      </listitem>

      <listitem>
       <para>
        Add <link linkend="tcn">tcn</link> (triggered change notification)
        to generate <command>NOTIFY</command> events on table changes
        (Kevin Grittner)
       </para>
      </listitem>

      <listitem>
       <para>
        Implement dry-run mode for <link
        linkend="pgarchivecleanup"><application>pg_archivecleanup</application></link>
        (Gabriele Bartolini)
       </para>

       <para>
        This only outputs the names of files to be deleted.
       </para>
      </listitem>

      <listitem>
       <para>
        Add new <link linkend="pgbench">pgbench</link> switches
        <option>--unlogged-tables</option>, <option>--tablespace</option>, and
        <option>--index-tablespace</option> (Robert Haas)
       </para>
      </listitem>

      <listitem>
       <para>
        Have <link linkend="vacuumlo">vacuumlo</link> remove large objects
        in batches, to reduce locking (Tim Lewis, Tom Lane)
       </para>

       <para>
        The new <option>-l</option> option allows changing the number of large
        objects removed per transaction.
       </para>
      </listitem>

      <listitem>
       <para>
        Change <link
        linkend="pgtestfsync"><application>pg_test_fsync</application></link> to test
        for a fixed amount of time, rather than a fixed number of cycles
        (Bruce Momjian)
       </para>

       <para>
        The <option>-o</option>/cycles option was removed, and
        <option>-s</option>/seconds added.
       </para>
      </listitem>

      <listitem>
       <para>
        Add a <link
        linkend="pgtesttiming"><application>pg_test_timing</application></link>
        utility to measure clock monotonicity and timing overhead (Ants
        Aasma, Greg Smith)
       </para>
      </listitem>

    </itemizedlist>

    <sect4>
     <title><link linkend="pgupgrade"><application>pg_upgrade</application></link></title>

     <itemizedlist>

      <listitem>
       <para>
        Allow <application>pg_upgrade</application> to upgrade an old cluster that
        does not have a <literal>postgres</literal> database (Bruce Momjian)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow <application>pg_upgrade</application> to handle cases where some
        old or new databases are missing, as long as they are empty
        (Bruce Momjian)
       </para>
      </listitem>

      <listitem>
       <para>
        Allow <application>pg_upgrade</application> to handle configuration-only
        directory installations (Bruce Momjian)
       </para>
      </listitem>

      <listitem>
       <para>
        In <application>pg_upgrade</application>, add <option>-o</option>/<option>-O</option>
        options to pass parameters to the servers (Bruce Momjian)
       </para>

       <para>
        This is useful for configuration-only directory installs.
       </para>
      </listitem>

      <listitem>
       <para>
        Change <application>pg_upgrade</application> to use port 50432 by default
        (Bruce Momjian)
       </para>

       <para>
        This helps avoid unintended client connections during the upgrade.
       </para>
      </listitem>

      <listitem>
       <para>
        Overhaul <application>pg_upgrade</application> logging and failure reporting
        (Bruce Momjian)
       </para>

       <para>
        Create four append-only log files, and delete them on success.
        Add <option>-r</option>/<option>--retain</option> option to unconditionally
        retain these files.  Also remove <application>pg_upgrade</application> options
        <option>-g</option>/<option>-G</option>/<option>-l</option> options as unnecessary,
        and tighten log file permissions.
       </para>
      </listitem>

      <listitem>
       <para>
        Adjust <application>pg_upgrade</application> environment variables (Bruce
        Momjian)
       </para>

       <para>
        Rename data, bin, and port environment
        variables to begin with <literal>PG</literal>, and support
        <envar>PGPORTOLD</envar>/<envar>PGPORTNEW</envar>, to replace
        <envar>PGPORT</envar>.  INCOMPATIBILITY?
       </para>
      </listitem>

      <listitem>
       <para>
        Have <application>pg_upgrade</application> create a script to incrementally
        generate more accurate optimizer statistics (Bruce Momjian)
       </para>

       <para>
        This reduces the time needed to generate minimal cluster statistics
        after an upgrade.
       </para>
      </listitem>

      <listitem>
       <para>
        Reduce cluster locking in <application>pg_upgrade</application> (Bruce
        Momjian)
       </para>

       <para>
        Specifically, only lock the old cluster if link mode is used,
        and do it right after the schema is restored.
       </para>
      </listitem>

    </itemizedlist>

    </sect4>

    <sect4>
     <title><link linkend="pgstatstatements"><application>pg_stat_statements</application></link></title>

     <itemizedlist>

      <listitem>
       <para>
        Allow <application>pg_stat_statements</application> to aggregate similar
        queries via SQL text normalisation (Peter Geoghegan, Tom Lane)
       </para>

       <para>
        Users with applications that use non-parameterized SQL will now
        be able to monitor query performance without using log file
        analysis.
       </para>
      </listitem>

      <listitem>
       <para>
        Add dirtied and written block counts and read/write times to
        <application>pg_stat_statements</application> (Robert Haas, Ants Aasma)
       </para>
      </listitem>

      <listitem>
       <para>
        Prevent <application>pg_stat_statements</application> from double-counting
        <command>PREPARE</command> and <command>EXECUTE</command> commands
        (Tom Lane)
       </para>
      </listitem>

    </itemizedlist>

    </sect4>

    <sect4>
     <title><link linkend="sepgsql">sepgsql</link></title>

     <itemizedlist>
      <listitem>
       <para>
        Support <literal>SECURITY LABEL</literal> on global objects (KaiGai
        Kohei, Robert Haas)
       </para>

       <para>
        Specifically, add <literal>SECURITY LABEL</literal>s to databases,
        tablespaces, and roles.
       </para>
      </listitem>

      <listitem>
       <para>
        Allow sepgsql to honor database labels (KaiGai Kohei)
       </para>
      </listitem>

      <listitem>
       <para>
        Perform sepgsql permission checks during the creation of various
        objects (KaiGai Kohei)
       </para>
      </listitem>

      <listitem>
       <para>
        Add <function>sepgsql_setcon()</function> and related functions to control
        the sepgsql security domain (KaiGai Kohei)
       </para>
      </listitem>

      <listitem>
       <para>
        Add a userspace access cache to sepgsql to improve performance
        (KaiGai Kohei)
       </para>
      </listitem>

     </itemizedlist>
    </sect4>

   </sect3>

   <sect3>
    <title>Documentation</title>

    <itemizedlist>

      <listitem>
       <para>
        Add a rule to optionally build docs with the stylesheet from the
        website (Magnus Hagander)
       </para>

       <para>
        Use <command>make STYLE=website draft</command>.
       </para>
      </listitem>

      <listitem>
       <para>
        Improve <command>EXPLAIN</command> documentation (Tom Lane)
       </para>
      </listitem>

      <listitem>
       <para>
        Document that user/database names are preserved with double-quoting
        by command-line tools like <application>vacuumdb</application>  (Bruce
        Momjian)
       </para>
      </listitem>

      <listitem>
       <para>
        Improve the foreign data wrapper <acronym>API</acronym> and
        documentation (Hanada Shigeru)
       </para>
      </listitem>

      <listitem>
       <para>
        Document the actual string returned by the client for MD5
        authentication (Cyan Ogilvie)
       </para>
      </listitem>

    </itemizedlist>

   </sect3>

  </sect2>
 </sect1>
