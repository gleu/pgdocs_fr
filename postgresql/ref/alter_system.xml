<?xml version="1.0" encoding="UTF-8"?>
<!--
doc/src/sgml/ref/alter_system.sgml
PostgreSQL documentation
-->

<refentry id="sql-altersystem">
 <indexterm zone="sql-altersystem">
  <primary>ALTER SYSTEM</primary>
 </indexterm>

 <refmeta>
  <refentrytitle>ALTER SYSTEM</refentrytitle>
  <manvolnum>7</manvolnum>
  <refmiscinfo>SQL - Commandes du langage</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>ALTER SYSTEM</refname>
  <refpurpose>Modifier un paramètre de configuration du serveur</refpurpose>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
ALTER SYSTEM SET <replaceable class="parameter">paramètre_configuration</replaceable> { TO | = } { <replaceable class="parameter">valeur</replaceable> | '<replaceable class="parameter">valeur</replaceable>' | DEFAULT }

ALTER SYSTEM RESET <replaceable class="PARAMETER">configuration_parameter</replaceable>
ALTER SYSTEM RESET ALL
</synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>Description</title>

  <para>
   <command>ALTER SYSTEM</command> écrit les valeurs du paramètre de
   configuration dans le fichier <filename>postgresql.auto.conf</filename>.
   Setting the parameter to <literal>DEFAULT</literal>, or using the
   <command>RESET</command> variant, removes the configuration entry from
   <filename>postgresql.auto.conf</filename> file. Use <literal>RESET
   ALL</literal> to clear all configuration entries.  The values will
   be effective after reload of server configuration (SIGHUP) or in next
    server start based on the type of configuration parameter modified.
  </para>

  <para>
   Cette commande n'est pas autorisée dans un bloc de transaction ou dans une
   fonction.
  </para>

  <para>
   Voir <xref linkend="config-setting"/> pour d'autres moyens de configurer les
   paramètres et de les prendre en compte.
  </para>
 </refsect1>

 <refsect1>
  <title>Paramètres</title>

  <variablelist>
   <varlistentry>
    <term><replaceable class="parameter">paramètre_configuration</replaceable></term>
    <listitem>
     <para>
      Nom d'un paramètre configurable. Les paramètres disponibles sont documentés
      dans <xref linkend="runtime-config"/>.
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><replaceable class="parameter">valeur</replaceable></term>
    <listitem>
     <para>
      Nouvelle valeur du paramètre. Les valeurs peuvent être spécifiées en tant
      que constantes de chaîne, identifieurs, nombres ou liste de valeurs
      séparées par des virgules, suivant le paramètre. <literal>DEFAULT</literal>
      peut être utilisé pour supprimer le paramètre et sa valeur du fichier
      <filename>postgresql.auto.conf</filename>.
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1>
  <title>Notes</title>

  <para>
    Cette commande nepeut pas être utilisée pour configurer le paramètre <xref
    linkend="guc-data-directory"/> et tous les paramètres qui ne sont pas
    acceptés dans le fichier <filename>postgresql.conf</filename> (par
    exemple <link linkend="runtime-config-preset">preset options</link>).
  </para>
 </refsect1>
  
 <refsect1>
  <title>Exemples</title>

  <para>
   Configurer le paramètre <literal>wal_level</literal>&nbsp;:
<programlisting>
ALTER SYSTEM SET wal_level = hot_standby;
</programlisting>
  </para>

  <para>
   Configurer le paramètre <literal>authentication_timeout</literal>&nbsp;:
<programlisting>
ALTER SYSTEM SET authentication_timeout = 10;
</programlisting></para>
 </refsect1>

 <refsect1>
  <title>Compatibilité</title>

  <para>
   La commande <command>ALTER SYSTEM</command> est une extension
   <productname>PostgreSQL</productname>.
  </para>
 </refsect1>

 <refsect1>
  <title>Voir aussi</title>

  <simplelist type="inline">
   <member><xref linkend="sql-set"/></member>
   <member><xref linkend="sql-show"/></member>
  </simplelist>
 </refsect1>

</refentry>
